@model WebApp.Models.Brand

<!-- Begin Page Content -->
<div class="container-fluid">

    <!-- DataTales Example -->
    <div class="card shadow mb-4">
        <div class="card-header py-3 d-flex justify-content-between align-items-center">
            <h6 class="m-0 font-weight-bold text-primary">Brand Tables</h6>
            <button class="btn btn-success" data-toggle="modal" data-target="#addModal">
                Add more
            </button>
        </div>
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th>Id</th>
                            <th>Image</th>
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tfoot>
                        <tr>
                            <th>Id</th>
                            <th>Image</th>
                            <th>Actions</th>
                        </tr>
                    </tfoot>
                    <tbody>
                        @foreach (var item in ViewBag.Brands)
                        {
                            <tr>
                                <td>@item.BrandId</td>
                                <td class="w-25">
                                    <div class="text-center">
                                        <img src="@item.ImageURL" alt="@item.BrandId" class="w-25">
                                    </div>
                                </td>
                                <td class="text-center">
                                    <a class="btn btn-warning" href="/dashboard/brand/edit/@item.BrandId">Edit</a>
                                    <a class="btn btn-danger" href="/dashboard/brand/delete/@item.BrandId">Delete</a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

            <!-- Add Modal-->
            <div class="modal fade" id="addModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
                aria-hidden="true">
                <div class="modal-dialog" role="document">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="exampleModalLabel">Add more</h5>
                            <button class="close" type="button" data-dismiss="modal" aria-label="Close">
                                <span aria-hidden="true">×</span>
                            </button>
                        </div>

                        <div class="modal-body">
                            @using (Html.BeginForm("Create", "Brand", FormMethod.Post, new
                            {
                                enctype =
                            "multipart/form-data"
                            }))
                            {
                                <div class="col-12 mt-3">
                                    <label for="fileUpload">Choose image:</label>
                                    <input type="file" name="fileUpload" id="fileUpload" accept="image/*" required
                                        class="form-control" />
                                </div>

                                <div class="modal-footer mt-3">
                                    <a class="btn btn-secondary" type="button" data-dismiss="modal">Cancel</a>
                                    <button class="btn btn-success" type="submit">Add</button>
                                </div>
                            }
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

</div>
<!-- /.container-fluid -->